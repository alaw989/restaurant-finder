includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 5
    paths:
        - app/
    # Exclude tests for now - we'll add them in a later spec
    excludePaths:
        - tests/
