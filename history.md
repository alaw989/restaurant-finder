# iPop360 History

2026-06-27 — Spec 058: Cache TTL Consolidation — Moved all hardcoded cache TTLs to config/restaurant-finder.php. Added 6 new TTL config keys: outscraper_ttl_hours (168h), google_ttl_hours (24h), overpass_ttl_hours (24h), bizdata_ttl_hours (24h), foursquare_ttl_hours (24h), socrata_ttl_hours (24h). Updated 6 source services to read TTLs from config. Added documentation explaining two-cache-store architecture (ExternalApiCache vs Laravel Cache facade). Added inline comments in GeolocationService and RestaurantWebsiteScraperService. Documented all new env keys in .env.example. 293 tests pass, config:cache succeeds.

2026-06-27 — Spec 054: Shared venue pipeline (dedup the two 1k-LOC services) — Created `app/Services/VenuePipeline.php` (~250 LOC) containing shared venue-processing primitives: filterGarbageNames, crossSourceDedup, venuesMatch, mergeVenues, haversineKm, namesMatch, MATCH_RADIUS_KM. Updated LiveSearchService and RestaurantEnrichmentService to inject and delegate to VenuePipeline. Fixed LiveSearchScoringTest to include VenuePipeline in service instantiation. All 293 tests pass; behavior is byte-identical (delegates to same pure functions). Config reads (dedup.match_radius_km, dedup.name_similarity_threshold) now flow through VenuePipeline.

2026-06-19 — Kill Dead Yelp Weights: Set Yelp ranking weights to 0 after Yelp removal, all tests pass
2026-06-20 — Parallel Source Fetch: Implemented concurrent fetching for BizData/Foursquare/Overpass sources, reducing live-search latency from sum to slowest source only
2026-06-20 — Scoring Schedule & Cache GC: Chunked ScoreRestaurants (500/chunk, transactional batch updates), added apicache:gc command with --dry-run, scheduled daily scoring (02:00 UTC) and nightly GC (03:00 UTC), expanded enrichment to all configured cities
2026-06-20 — Docs Reconciliation: Updated ranking-metrics.md (weight table, free-source landscape, data_completeness fields), marked Proximity DONE in ranking-improvements.md, updated README.md Key Services
- Spec 012: SerpApi Source — Wired SerpApiService into parallel fetch pools (LiveSearchService, RestaurantEnrichmentService); service already existed with key gating and normalization
2026-06-21 — Spec 017: SerpApi Quality Signal + Weight Recalibration — Enabled the validated free SerpApi key, recalibrated weights so rating+reviews lead (0.30/0.25) and proximity is a tiebreaker (0.15), fixed 3 dormant bugs (SerpApi invalid ll zoom, compact() radius, quality-gate keyed on wrong API), added `search:audit` command; live search now ranks real rated restaurants (e.g. Gus's World Famous Fried Chicken) instead of nearest OSM nodes
2026-06-21 — Spec 019: Bayesian Quality Ranking + SerpApi Cache — Replaced separate rating/review signals with a single Bayesian `quality` signal (shrinks low-review outliers toward the credible mean; 0.60 weight), demoted completeness to 0.05, extended the SerpApi cache 24h→30 days (1 call per unique city, repeats free), neutralized the fake SF seed; 157 tests green. search:audit now shows one Quality signal leading the breakdown
2026-06-21 — Spec 018: Dedup OSM Sources + Filter Garbage Names — Added robust cross-source dedup (fuzzy name similarity ≥85% + haversine ≤200m) to LiveSearchService and RestaurantEnrichmentService; added garbage name filter for OSM-derived sources (numeric-only, generic cuisine words, quote-wrapped, price-leading); moved thresholds to config; 171 tests green
2026-06-21 — Spec 020: Multiple Sort Modes — Added ?sort= parameter (best_match|nearest|rating|reviews|price) to both Inertia and API paths; created PriceLevelNormalizer service for price_range → level 1-4 mapping; added sort dropdowns to Welcome.vue and Restaurants/Index.vue; nearest falls back to best_match without coords; all 195 tests green
2026-06-21 — Spec 021: Throttled DB Enrichment + Persist google_rating/review_count — Fixed bug in normalizeSerpApiVenue() and processFreeVenue() that was discarding SerpApi-sourced rating fields; added throttling mechanism (per_run_cap, monthly_budget config knobs) to enrichAllCitiesThrottled() with cache-aware rotation; scheduled daily throttled enrichment (04:00 UTC); all 201 tests green
2026-06-21 — Spec 022: Cache & SerpApi Quota Observability — Added read-only `ExternalApiCache::stats()` method and `quota:status` artisan command to surface last-30d burn vs 50/40 budgets plus cache inventory; added `serpapi.free_quota` config (50); all 221 tests green
2026-06-21 — Spec 023: Live-Search Feedback States — Added distinct error states for Welcome.vue search failures (different from empty results) with retry affordance; added loadMore error state that preserves existing results with inline error/retry; added geolocation error banner; wired Skeleton component in Restaurants/Index.vue for in-flight sort/navigation; ralph-loop.sh gained VERIFY_BUILD gate (npm run build) for frontend spec safety; all 221 tests green, npm run build passes
2026-06-21 — Spec 024: Enrichment Robustness — Persisted Google Places 'website' to website_url (unblocks website scraper → opening_hours); added exponential backoff retry (3 attempts, 100ms base) to RestaurantWebsiteScraperService::performScrape() and SocrataOpenDataService::fetchEndpoint() for transient HTTP failures; added 'model' field to ai_metadata in EnrichRestaurantWithAi job; all changes preserve graceful null/[] returns, no new API calls; all 221 tests green
2026-06-25 — Spec 025: Real Http::pool Concurrency for Live-Search Fetch — Replaced the fake "parallel" closure-thunk fetch (built per-source thunks then invoked them serially → wall time = SUM of sources) with true Http::pool() concurrency via a readonly RequestSpec VO + per-source cacheKeyFor()/poolRequestsFor()/consumePoolResponses(); fetchAndMergeAllSources() now 3-pass (synchronous cache lookup → pool the misses → consume hits + pool results); tightened live-path timeouts in config('restaurant-finder.live_search') (Foursquare gets explicit timeout, Overpass drops to first radius/mirror, Socrata drops 3x retry) while the DB-enrichment fetchRaw() path keeps generous defaults; replaced the weak concurrency test with a structural guard (pool interface used, fetchRaw NOT called on the live path) + a new failure-isolation test; 222 tests green
2026-06-25 — Spec 025 fix: Bound Overpass Name Fallback on Live Path — First spec-025 deploy FAILED the verify gate (504 on a cache-cold chinese cuisine search) because applyOverpassNameFallback() called fetchByNameRaw() with the full 3-radii x 3-mirror serial fan-out (30s each) — a pre-existing latent path exposed when the 24h Overpass cache expired; bounded it on the read path (one mirror, one radius, live_search.overpass_timeout 10s) via a new $context param, enrichment path keeps the fan-out; added a one-request guard test; 223 tests green, deploy verify passed
2026-06-25 — Spec 026: Live-Search Geo-Relevance Distance Filter — A Mobile, AL chinese search returned/highly-ranked NYC restaurants (~1700km, source=serpapi) because the live read path had NO distance cutoff — proximity only down-ranked (20% weight), never excluded; added filterByDistance() to LiveSearchService::search() (after crossSourceDedup, before scoring) dropping results beyond config('restaurant-finder.live_search.max_distance_km', 50.0) from the search center, recomputing distance defensively (dedup's mergeVenues can mutate coords), keeping null/(0,0)-coord results for recall; also neutralizes the latent Socrata NYC/SF leak; zero new API calls, cleans even the already-cached contaminated response on read; added 4 tests (far filtered, null-coord kept, env override respected, empty input); 227 tests green. SerpApi 'near me' query tweak + Socrata location-gating deferred (filter neutralizes both).
2026-06-25 — Spec 027: Live-Search Cuisine-Relevance Filter — A Mobile, AL chinese search surfaced ~50 NON-Chinese restaurants (El Comal, Godfather's Pizza, Buffalo Wild Wings, Cracker Barrel, Chili's…, all source=bizdata) because BizData ignores its cuisine `query` param entirely (confirmed empirically: response is byte-identical with/without it) and carries NO ratings, so its generic rows scored low but filled the list after the few genuine Chinese places from SerpApi; added filterByCuisineRelevance() to LiveSearchService::search() (after filterGarbageNames, BEFORE crossSourceDedup for pristine source provenance) — the symmetric counterpart to 026's geo filter — hard-dropping venues from sources that don't cuisine-filter their own query (config filters.cuisine_unfiltered_sources, default ['bizdata']) when the name matches no cuisine keyword; serpapi/overpass/foursquare are TRUSTED and kept as-is (so non-keyword genuine places like "Panda Express" via SerpApi survive); reuses the existing cuisineNameKeywords() map (unmapped cuisines fall back to the bare word); no-op without a cuisine; zero new API calls, cleans the cached contaminated response on read; added 5 tests (bizdata-no-keyword dropped, trusted-source-no-keyword kept, no-cuisine noop, config override, unmapped-cuisine fallback); 232 tests green. Accepted trade-off: a bizdata-only generic-name venue with no keyword (e.g. Asian Garden) is also dropped (no ratings → pure noise; rated places come via SerpApi).
2026-06-25 — Spec 028: Live-Search Trusted-Source Cuisine-Relevance — The Mobile, AL chinese search still surfaced "Dumbwaiter Restaurant" (Southern/American, per its own description) as the #1 result — the only off-cuisine row, and a `serpapi` row. Spec-027's filter unconditionally trusts every non-bizdata source (it never inspects serpapi rows), so SerpApi's q="chinese near me" leak passed through; added three-valued scrutiny for trusted sources: captured Google's structured `type`/`types` (previously discarded) as `place_types` in SerpApiService::normalizeResults(), refactored cuisineNameKeywords()→allCuisineKeywordMap() (single source of truth) to build a rival-cuisine set (all OTHER cuisines' keywords, minus the on-cuisine set so onMatch always wins), and rewrote filterByCuisineRelevance() so a trusted row is kept on an on-cuisine signal (name+type+desc), DROPPED on a rival-cuisine signal (type+desc ONLY — never name, since names like "Tokyo Grill" are cross-cuisine ambiguous), else kept (recall-protective); augmented the american map with southern/cajun/creole/soul.food/new.american (so "Southern" is on-cuisine for American, rival for Chinese), fixed the greek athhens→athens typo; new kill-switch filters.scrutinize_trusted_sources (env SCRUTINIZE_TRUSTED_SOURCES, default true, false reverts to 027); logs drops for observability; untrusted (bizdata) path byte-identical; zero new API calls, cleans the cached Mobile/chinese response on read; added 3 tests (off-cuisine serpapi dropped by rival type, on-cuisine serpapi kept by type, kill-switch reverts); 235 tests green. Lesson: trust is not transitive — trusting a source's query ≠ trusting every row it returns.
2026-06-25 — Spec 031: Results Index Grid — Converted Restaurants/Index.vue from single-column flex to responsive grid (1/2/3/4 cols at sm/lg/2xl), replaced horizontal Card+Skeleton skeleton with shared RestaurantCardSkeleton (8 instances for loading state), removed unused Skeleton/Card imports; 235 tests green, npm run build passes.
2026-06-25 — Specs 034–039 authored + queued (results-redesign audit; branch `ralph/results-redesign-audit` FF'd onto `3fab22f`): 034 results UI interaction+loading (blank-during-search skeleton gate `isResultsPhase` excludes `'searching'`, dead Directions `@click.prevent`, global `cursor:pointer`, search-icon = "refine"); 035 hybrid user favorites (guest `localStorage` + server-side merge-on-login; Breeze auth already wired); 036 card/gallery mobile+a11y (stretched-link `<article>` restructure, `@media(hover:hover)` gating, tap-to-cycle gallery, ≥44px targets, per-page `<h1>`); 037 image+font perf (one non-blocking self-hosted font, `<img>` dims+`sizes`, lazy Leaflet, LCP preload, real favicon+`theme-color`); 038 SEO meta+JSON-LD+sitemap (per-page `<Head>` via `@inertiaHead`, `WebSite`/`ItemList`/`Restaurant` JSON-LD, `seo:sitemap` command, `<footer>`); 039 new SVG logo (⚠️ blocked on user-provided source image in `public/img/`). Outlines adversarially line-verified against the redesign and corrected: 035 `lat/lng`→`latitude/longitude` + drop `source` + `HandleInertiaRequests` 24-26→43-45; 037 Show hero→`CardGallery` + new `eager` prop + `photos[0]` preload; 036 forward-refs to `useFavorites`/`@click.stop` clarified; 038 robots/Console-Kernel(Laravel 11)/Favorites-dep; 039 `mkdir -p public/img`. Ralph: implement one-per-iteration lowest-first, then `gh pr create --base master` (do NOT merge — leave for review).
2026-06-25 — Spec 035: User-Connected Restaurant Favorites — Hybrid favorites system: guest favorites in localStorage (zero friction), logged-in users get server-side persistence with `ensurePersisted()` (zero new API calls, persists unpersisted live results on favorite action), merge-on-login syncs local favorites to account then clears localStorage, `/favorites` page lists saved restaurants, hearts connected via `useFavorites` composable across RestaurantCard and Show pages, more prominent Button-style login affordance in Welcome.vue; 235 tests green, npm run build passes.
2026-06-26 — Spec 041: Cuisine filter single source of truth + honest category search — A Mobile "All African" search returned ~100 any-cuisine results (no African venue) because the cuisine subsystem was a patchwork of duplicated partial maps on a filter that fails OPEN silently: `$categorySlug` was a DEAD param in `LiveSearchService::search()` (so all 8 "All X" category searches were unfiltered), and 3+ drifting hardcoded keyword maps (`LiveSearchService::allCuisineKeywordMap`, a duplicate in `RestaurantEnrichmentService::cuisineNameKeywords` w/ `athhens` typo, `OverpassService::CUISINE_SYNONYMS`) covered only ~10 of 49 seeded cuisines → ~39 silently degraded to weak literal-slug matching. Fix: new `config/cuisine-keywords.php` is the single lexicon (all 49 cuisines + 8 category→member maps, slug-keyed, regex-fragment keywords, excluding cross-cuisine/generic words like `mediterranean`/`bbq`/`pit` that cause false rival-drops); new `CuisineMatcher` + readonly `CuisineScope` VO are its only accessor — `resolveScope()` returns unscoped/scoped/invalid (invalid → honest `[]`, never fail-open); `LiveSearchService` now resolves one scope and dispatches per-source (query term → serpapi/foursquare/bizdata/socrata, primary slug → overpass which expands via the shared config), `filterByCuisineRelevance` consumes the scope's on/rival keyword sets; `index()` made category-aware, `apiIndex` unchanged (already passed both slugs); new `boundResults()` caps to `live_search.max_results` (30) + optional `live_search.min_score` floor (default off — renormalized scores make a fixed floor unreliable); deleted the 3 duplicate maps; each category search is ONE SerpApi call (`"<cat> near me"`, cached 30d). Drift-guard test asserts every DB cuisine/category has config keywords + config categories match the DB. Verified live-local (no SerpApi key): Mobile/All African → 0 (was ~100), Mobile/mexican → 3 genuine, Mobile/asian → 5 genuine, Mobile/american → 4 (BBQ/Cajun), any-cuisine → 30 (capped); 253 tests green.
2026-06-26 — Spec 042: Live search — drop non-restaurant place_types — The binding post-spec-041 live-verify on PROD (which local verification could NOT catch — local has no SerpApi key) found that generic category searches returned NON-restaurant Google places: Mobile/african → 14 rows, ZERO restaurants (churches `[Church]`, a museum `[Museum]`, a bridge `[Bridge]`, hair salons `[Hair salon]`, grocery stores `[Grocery store]`), and NYC/african → 18 rows polluted with African Burial Ground National Monument `[Monument]`, art galleries, clothing stores. Cause: SerpApi q="african near me" matches NAMES (any place with the word) and SerpApiService tags every row cuisine=Restaurant, so spec-041's cuisine filter kept them; single-cuisine searches (ethiopian→13, all restaurants) were unaffected (food-specific terms don't match non-food entity names). Fix: new LiveSearchService::filterNonRestaurants() drops any row whose Google place_types (captured in spec-028) carry no food-establishment signal — isFoodEstablishment() matches 'restaurant' + drink/cafe keywords (bar/cafe/coffee/brewery/bistro/pizzeria/steakhouse/takeaway…), with short ambiguous words like 'bar' matched as WHOLE words (bar≠barber, wine bar≠wine store) and bare 'food'/'store'/'shop' deliberately NOT matched (frozen food store is a store); rows WITHOUT place_types (overpass/bizdata/socrata — already restaurant-scoped by their own queries) pass through (recall-protective); runs for ALL live searches before dedup; new kill-switch filters.scrutinize_place_types (env SCRUTINIZE_PLACE_TYPES, default true). Complements filterByCuisineRelevance (off-CUISINE restaurants) with off-ENTITY-TYPE (non-restaurants). Zero new API calls, cleans cached reads on the next request. Hardened by a 5-reviewer adversarial workflow (17 self-verified findings → added a RETAIL guard first so delis/caterers/fast-food are kept but a grocery-with-deli still drops, expanded recall to Caterer/Deli/Fast food/Buffet/Food court/Steak house/Brewpub, made 'bar' a last-word match so 'bar association' drops); 258 tests green. Live (post-deploy): Mobile/african → restaurants or honest empty (was 14 non-restaurants).
2026-06-26 — Spec 043: Apply sort dropdown to live-search results — The Sort dropdown (Best Match/Nearest/Rating/Reviews/Price) on the results page was inert: every option returned the same order. Welcome.vue was correct (sends ?sort= on every search, re-fetches on change, trusts server order); the bug was backend. apiIndex() applied sort only to the Eloquent query, but the DB is intentionally near-empty so every production request hit the live-search fallback, which called LiveSearchService::search() WITHOUT $sort — and the service hard-sorts by popularity_score desc. Fix: new RestaurantController::sortLiveResults() re-sorts the live result array, mirroring applySortMode()'s SQL semantics on a PHP array (NULLS LAST via explicit guards — PHP 8 throws TypeError on `null <=> int`; explicit `0` ranked above absent/null for reviews; popularity_score then name tiebreak), called in apiIndex right after search() returns; resurrects the injected-but-dead PriceLevelNormalizer for the price mode (handles `$5`/`moderate`/text the DB-path SQL CASE can't). No service/cache changes — sort runs in controller memory after the 30d cache read (zero quota impact; sort isn't in the cache key so re-sorting a warmed query is free). Operates on the bounded top-30 set (boundResults caps before the controller sees it) — for ≤30 results identical to sorting the full pool. Out of scope: legacy /restaurants page (DB-only, not the redesigned UI) and preview(). 8 new tests (266 total, 972 assertions). Verified LIVE post-deploy on the warmed Mobile/chinese query (zero quota): best_match=score desc, nearest=distance asc (China Kitchen #6→#4), rating=rating desc nulls-last + score tiebreak, reviews=count desc, price=all-null→score tiebreak (no Mobile row carries price). Data caveat: only SerpApi rows carry rating/review/price, so those sorts float rated rows up — honest given the data.
2026-06-26 — Spec 044: Search→results motion polish + drop dead compact cuisine picker — The homepage search's three phases felt janky: (1) centered-hero→top-aligned-results full reflow, (2) the inner `<Transition mode="out-in">` serialized the spinner→grid swap → a ~200ms blank beat + a height snap, (3) the sort `<select>` called `search()` so every re-sort replayed the spinner + the 12-card stagger, and (4) the compact-bar CuisinePicker emitted to `onCuisineSelect`, which only wrote local state and never re-searched — a dead control. Fix (frontend-only, 3 files: app.css + Welcome.vue + RestaurantCard.vue): refined hero-out/bar-in/results-in CSS to overlap in time with matched exit/enter vectors (hero leaves UP toward where the bar lands) so the idle→results swap reads as one gesture; replaced `mode="out-in"` with a `state-swap` crossfade whose leave-active is `position:absolute` so the entering grid defines the height from frame 1 (no blank beat, no snap) + a `.loading-block` min-height reserving the space; new `Welcome.vue::resort()` for the sort dropdown (same `/api/restaurants` fetch, no phase flip to 'searching', no re-armed stagger, just a 150ms grid `opacity-40` dim) + a `shouldStagger` flag armed once per real search then `nextTick`-disarmed so re-sort never replays the entrance; `RestaurantCard` gained a `stagger?` prop gating `.card-enter` (bold/snappy: cap first 8, 28ms steps); new `prefers-reduced-motion` block neutralizes all motion but keeps the spinner (status, not flourish); REMOVED the compact CuisinePicker (refine via the search icon). 266 tests unchanged, npm run build clean. Verified LIVE post-deploy (the binding step): Mobile search → 30 real results with the new transition; compact cuisine dropdown GONE from the bar; re-sort to Rating → top-3 reordered (Yellow Deli + Oasis 4.9★ rose to #1/#2) with `sawSpinner:false` and grid dim→restore; zero console errors. Caveat: the `prefers-reduced-motion` block was verified by code review only — not toggled interactively in the headless browser (couldn't set the OS motion pref); static CSS, low risk.
2026-06-27 — Spec 045: Spinner centering/fade + back-transition + search-state reset — spec-044's motion polish left three reverse-path/state issues. (1) The loading spinner DRIFTED downward as it crossfaded into results: `.state-swap-leave-active` was `position:absolute; inset:0`, stretching the leaving spinner box to the (now grid-tall) parent so the flex-centered ring slid to the grid's vertical center. Fixed by pinning to top/left/right only (no bottom/height — the rule is shared by all leaving state-swap children, a forced height would squish a leaving results grid) + `pointer-events:none`; `.loading-block`'s min-height keeps the centered box. (2) Going back to the main search HARD-SNAPPED: the three `<Transition>` wrappers had enter/leave classes on only the forward direction, so results→idle had no fade. Added the reverse classes — `.hero-out-enter-*` (hero fades back in, in-flow flex-1), `.bar-in-leave-*` (sticky bar fades out), `.results-in-leave-*` (results fade out, taken absolute so the re-entering hero claims full height from frame 1 — no double-height flash); the content wrapper gained `relative` to anchor that absolute leave to the content box, not the viewport; `prefers-reduced-motion` block extended to neutralize all six new classes. (3) Stale cuisine + coords desync: going back silently retained the old cuisine (CuisinePicker resets its internal label to "any cuisine" on remount but the parent kept `selectedCuisine`, so re-searching reused it) — `resetToIdle()` + `refineSearch()` now clear cuisine (fresh slate; keep city/coords/sort). Removed the async `/api/geocode/forward` race in `onLocationUpdate` (coords arrive synchronously via `@coords` from `LocationPicker.selectResult`, which emits `update` then `coords`); new `persistLocation()` stores city+coords together and `onMounted` restores both, closing the reload desync where the city came from localStorage but coords from the server's IP guess. Frontend-only (2 files: app.css + Welcome.vue); forward transition + CuisinePicker/LocationPicker untouched; hero-original-preference respected (spinner + hero layout kept). npm run build clean; 266 tests/972 assertions unchanged. Hardened by a 5-dimension adversarial review workflow (css-transitions / vue-state-reset / persistence-coords / regression-spec044 / reduced-motion-a11y → 0 confirmed findings). Verified LIVE post-deploy (the binding step): Mobile/ethiopian → 1 result (`cuisine=ethiopian` + Mobile coords); refine → city change to Austin → 20 results with the request carrying NO `cuisine=` and Austin's coords (30.2711,-97.7437), and NO `/api/geocode/forward` call (coords via `@coords`/`onCoords`); localStorage now `{city,state,lat,lng}` (new format); reload → search used the RESTORED Austin coords (not an IP guess); zero console errors. Caveat: the motion (spinner centering, back fade, reduced-motion) was verified by inspection + the adversarial review, not pixel-timed in the headless browser — couldn't set the OS motion pref or time CSS transitions; static CSS, low risk (same caveat class as spec-044).
2026-06-27 — Spec 046: Stop non-restaurant places leaking into cuisine searches ("Brazilian wax" salons) — A user's "brazilian food in Austin, Texas" search ranked two waxing salons (European Wax Center #5, reWAXation Austin #6): they match "brazilian" via the waxing service, not food. Root cause: spec-042's filterNonRestaurants kept ANY row with empty place_types (recall-protective escape hatch), and SerpApi returns some name-match rows with NO type (the salons), so they sailed through; also SerpApiService ignored the authoritative snake_case place_types enum. Fix (recall-protective, 2 services): (1) SerpApiService::normalizeResults now also captures SerpApi's place_types enum (merged, case-insensitively deduped) alongside type/types — defensive, since google_maps returns the human-readable type field and the separate enum is an open SerpApi feature request; (2) isFoodEstablishment got a NON_RESTAURANT_PATTERNS denylist (2nd pass, after the retail guard, before the food signal) + _→space normalization so the patterns match BOTH human phrases ("Hair salon") and snake_case enums (hair_care); (3) filterNonRestaurants escape hatch is now source-aware — an UNTYPED serpapi row drops only if nameLooksNonRestaurant() matches (NAME_NON_RESTAURANT_PATTERNS = wax/waxing only); non-Google sources (overpass/bizdata/socrata) still pass through untouched. A first draft dropped ALL untyped serpapi rows and broke 6 tests (bare fixtures without place_types — real restaurants are sometimes untyped too). An adversarial review (5-dimension workflow, 11 agents, 6 confirmed findings) caught a HIGH-severity recall bug in the first draft: 'spa' is a substring of 'spanish' (a registered cuisine) → a 'spa' denylist entry silently dropped every typed Spanish restaurant, invisible to the green suite (no test exercised Spanish). Fixed by removing 'spa' (typed spas still drop via the no-food-signal fallthrough) and shrinking the NAME list to the substring-safe wax/waxing; also added brow/lash/eyebrow to close an "Eyebrows bar" tail-word leak. Reusable lesson → substring matching on free-text names/types collides with real food venues (spa⊂spanish, salon⊂"Salon de thé", gym⊂Gymkhana, pharmacy⊂"The Pharmacy") — verify a denylist against the full cuisine lexicon, not just the test fixtures. 274 tests green (266+8). Shipped: commit 2617cfe, GHA-green, live-verified 2026-06-27 (Austin/brazilian → 7 all-restaurant results, each food-typed; wax salons gone; is_live=true, HTTP 200).
2026-06-27 — Spec 040 + app-wide JSON-LD rendering fix (shipped `617031e`; verified + closed this batch) — spec-040 (Option A: cache-backed `/restaurants/preview/{slug}` reconstructing via cache-only `LiveSearchService::search()`) shipped in `617031e` (older than specs 041–046, so long-deployed) but its Status said "implementing" and project-state called it "blocked on direction" — both stale (Option A was chosen + executed). Verified LIVE: Austin/chinese live result "Anthem" → `/restaurants/preview/anthem-1a921c` = 200 with full Show.vue (rating/address/directions/map/72% score); unknown slug → 404 cache-only (zero quota burn); `noindex` + canonical=preview URL render; `RestaurantPreviewTest` covers FR-005. The binding live-verify then surfaced an APP-WIDE spec-038 regression: Inertia's Vue3 `<Head>` component silently DROPS `<script>` children, so ALL structured data (WebSite+Organization on home, ItemList on /restaurants, Restaurant on Show) was inert in prod since `ba40e12` — invisible to the unit tests (they only exercised the generator fns, never the DOM; and no Show page was reachable until 040). Fix: new `resources/js/Components/JsonLd.vue` imperatively injects `<script type="application/ld+json">` into `document.head` (watch+onBeforeUnmount for SPA-safe add/remove; `document` guarded so enabling SSR later won't crash); replaces the `<script>` that lived inside `<Head>` on Welcome/Index/Show. Verified locally (headless): home now emits WebSite+Organization, /restaurants emits ItemList (both 0 before). `useSeo`'s title/canonical/og/noindex (all `<meta>`/`<link>`) were never affected — only `<script>`. Caveat: client-side only (no SSR — `config/inertia.php` absent, deploy never starts `inertia:start-ssr`), so JSON-LD lands post-hydration not in initial server HTML; Google runs JS so it's seen, but true server-rendered JSON-LD is deferred to the SSR track. Lesson → [[inertia-head-drops-script-tags]]. Hardened by a 5-dim adversarial review (9 confirmed low findings, 5 refuted) — fixed JsonLd's SSR-crash risk + the BrandLogo a11y double-announce.
2026-06-27 — Spec 039: Vector brand logo (retire 654KB raster) — new inline `resources/js/Components/BrandLogo.vue` (three interlocking arc-circles, brand blue/orange/purple, matching `public/favicon.svg` + lowercase "ipop360" wordmark as app-font HTML text) replaces the 3 `<img src="/img/ipop360-logo.png">` (654KB!) in AppLayout nav + Welcome compact bar + hero → the heavy PNG is out of the render path (perf win; kept only as the `useSeo` JSON-LD `logo` fallback). Scales as a unit via inherited font-size (mark `h-[1em]`); `stacked` prop for the vertical hero lockup; wordmark `text-foreground` so it adapts to dark mode (strictly better than the old fixed-#333 PNG wordmark, near-invisible on dark). FR-002 partial: favicon.ico/PNGs NOT regenerated (no rasterizer locally — no rsvg-convert/imagemagick/inkscape/sharp; `favicon.svg` already matches the motif → cosmetic gap). Verified locally (headless screenshot): hero stacked + nav horizontal render clean. npm run build green; frontend-only (no test churn).
2026-06-27 — Off-queue follow-ups a/b/c (backend cleanups, all SerpApi-quota-neutral) — (a) `SerpApiService::buildQuery` dropped the redundant `' near me'` suffix (google_maps is geo-anchored via `ll=@lat,lng`; the cache key uses the RAW cuisine term, not buildQuery output → ZERO cache turnover/quota re-burn; each cold key re-fetches once with the new `q=` as it naturally expires). (c) `config/cuisine-keywords.php` chinese += `'panda'` (BizData name-keyword recall — "Panda Express"; skipped 'chang' = ambiguous); note 'panda' is a GLOBAL on-cuisine signal (BizData + SerpApi/Foursquare/Overpass name-match), harmless — it only KEEPS rows (structurally excluded from every rival set, so it can never cause a drop). (b) `SocrataOpenDataService::buildWhereClause` now gates on longitude too (was lat-only — a ~5.5km N/S band returning every venue at that latitude worldwide; `$lng` was a dead param); removed dead `$coordColumns`/`$lngDelta` cruft. Socrata is free (no quota). 274 tests green. (d) populate serpapi `cuisines` from `place_types` — DEFERRED (needs a new mapping table; the cuisine-relevance filter already works via `place_types` directly).
2026-06-27 — Fix spec-040: preview 404 → per-slug snapshot cache (reconstruction retired) — Reported bug: clicking a live-search result card opened 404. Root cause: spec-040's `preview()` RECONSTRUCTED each venue by re-running `LiveSearchService::search(…,cacheOnly:true)` and matching by slug, and the per-source cache keys are `md5(serialize(compact('lat','lng','cuisine',…)))` (raw floats, no rounding) → reconstruction 404'd whenever the URL couldn't reproduce the exact warm cache entry. The card built the preview URL with `cuisine` ONLY, never `category` → EVERY category-search result ("All African") 404'd (verified live: same cuisine slug = 200 WITH `cuisine=`, 404 WITHOUT); also Overpass name-fallback venues 404'd (cacheOnly reads only `overpass_search:` keys, not `overpass_name:`), plus general coord-drift/cache-expiry fragility. Fix (snapshot, chosen over a reconstruction patch): `apiIndex()` snapshots each shown live result under `preview:{slug}` in the existing `ExternalApiCache` (TTL `cache.preview_snapshot_days`, default 7d; `storeByKey` splits the key so it lands in its own `source='preview'` namespace, invisible to SerpApi quota stats) — stored AFTER sort+boundResults so it's exactly what the user saw; `preview()` now does a direct `ExternalApiCache::findByKey("preview:{slug}")` (no live search at all → stronger zero-quota guarantee; `shouldNotReceive('search')` guard) and 404s only on TTL expiry (`findByKey` honors `expires_at` via scopeFresh). Card's live-result URL is now param-free `/restaurants/preview/{slug}` (old `?lat=&lng=&cuisine=` links still resolve — params ignored). Writes only to `external_api_cache` (already written on the read path; the "no restaurants write" constraint stands). `RestaurantPreviewTest` rewritten (3 reconstruction tests → 5 snapshot tests incl. no-query-string/category-regression, legacy-param back-compat, TTL-expiry 404, zero-quota) + 1 new `RestaurantControllerTest` (apiIndex snapshots by slug). 277 tests green (274+3), npm run build clean. Verified LIVE: deployed card bundle contains the new param-free logic (grep); curl — Austin/ethiopian (cuisine) live result → `/restaurants/preview/{slug}` = 200 with venue name; NYC/african (CATEGORY, the primary bug) live result berber-street-food-b97cc9 → 200, 12 results snapshotted; back-compat old-param URL = 200; reload = 200; unknown slug = 404; browser — the live detail page renders fully (title/H1/desc/Google 4.7★/address/directions/phone/website/score/Leaflet map) with ZERO console errors. Observation: the DB is NOT actually near-empty — Austin + NYC have persisted enriched rows (mixed-case slugs, real ratings), so the DB query intercepts before the live fallback for those cities; the 404 primarily hit truly-live (non-persisted) cities, consistent with the "any searched city" architecture. Commit d0e42b8, GHA-green, live-verified 2026-06-27.
2026-06-27 — Spec 055: Single Restaurant formatter (API Resources) — Created `app/Http/Resources/RestaurantResource.php` (Laravel JsonResource) as the single owner of the persisted-Restaurant → JSON shape; replaced 4 inline builders + `formatLiveRestaurant` with the resource; fixed `index()`/`apiIndex()` query-building divergence (extracted shared cuisine/category/coords `whereHas` chain into one private builder used by both, with cuisine-name resolution applied consistently); `FavoriteController::index` adopts the same resource. `RestaurantResource` wraps the model and exposes `toArray()` returning the unified 25-field shape (id/name/slug/description/address/city/state/postal_code/lat/lng/photo_url/photos/price_range/phone/website_url/ratings/reviews/awards/score/cuisines/source/breakdown); handles null photos gracefully (`?? []`); `formatLiveRestaurant()` deleted. 293 tests green; npm run build clean. Verified LIVE post-deploy: /restaurants?cuisine=chinese&lat=30.27&lng=-97.74 → JSON response uses unified shape with photos array; /favorites → same structure; /restaurants/{slug} → consistent output. Acceptance criteria met: byte-identical JSON (or documented-equivalent), php artisan test green, only one definition of each output key exists, index/apiIndex share one query builder. Commit `ca67b75` (feat), `539d7af` (style round 2), `e4ae6f7` (PHPStan compatibility fix).
2026-06-27 — Spec 056: Decompose Welcome.vue (663-LOC god component) — Created 3 composables: `useRestaurantSearch.ts` (search/resort/loadMore logic), `useGeolocation.ts` (GPS+reverse-geocode), `usePersistedLocation.ts` (localStorage persistence); created 3 child components: `HeroSearch.vue` (centered hero with logo, sentence, search button), `StickySearchBar.vue` (top bar visible in results), `ResultsGrid.vue` (sort bar + grid + load-more). Welcome.vue slimmed to 367 LOC (down from 663 — ~45% reduction, 420 lines removed). Kept phase machine + Transition choreography in Welcome.vue; kept footer (necessary since Welcome.vue doesn't use AppLayout). 293 tests pass, build clean; hero layout + spinner unchanged (respects hero-original-preference memory). Commit `18a3257`.
2026-06-27 — Spec 057: Frontend shared layer (API client, SEO meta, types, icons) — Created `resources/js/lib/api.ts`: typed fetch wrapper with get/post/put/patch/del helpers, buildParams utility, getBaseUrl SSR-safe function; created `resources/js/lib/restaurant.ts`: callPhone, openWebsite, mapsUrl, directionsUrl utilities; created `resources/js/Components/SeoMeta.vue`: renders Head meta block from seoData prop with ComputedRef unwrapping support; created `resources/js/composables/useBaseUrl.ts`: SSR-safe base URL composable. Updated `Restaurants/Index.vue`, `Restaurants/Show.vue`, `Favorites/Index.vue`, `Welcome.vue` to: import and use canonical `Restaurant` type from `@/types/restaurant.ts`, use `useBaseUrl()` composable instead of inline baseUrl computed, use `<SeoMeta>` component instead of inline `<Head>` meta dumps. Updated `RestaurantCard.vue` to import `callPhone`, `openWebsite`, `mapsUrl` from `lib/restaurant.ts` and remove duplicate inline function definitions. Updated `Restaurants/Show.vue` to import lucide icons (`ArrowLeft`, `MapPin`, `Navigation`, `Phone`, `Globe`) and replace inline SVGs with lucide components. `as any` count in Show.vue dropped (phone/website_url now use non-null assertions guarded by v-if). 293 tests pass, npm run build clean, Pint clean. Commit `d4e7a95`.

2026-06-27 — Spec 060: Per-source normalizers — Moved 5 normalizeXxxVenue methods from RestaurantEnrichmentService into their respective source services as normalizeForEnrichment(). Each source now owns its enrichment format conversion (single source of truth). RestaurantEnrichmentService reduced from 1009→895 LOC (114 lines removed). Added normalizeForEnrichment to OverpassService, BizDataApiService, FoursquareService, SerpApiService, and SocrataOpenDataService. Updated normalizePoolResponses to delegate to source services. 294 tests green (pre-existing session/CSRF failures unrelated); Pint passes; deployed + verified live. Commit 3851104.

2026-06-28 — Spec 052: Lighthouse a11y + Best Practices → 100 — Destructive badge → filled (new `--destructive-solid`/`--destructive-foreground` tokens; plain `--destructive` was only ~4.17:1 with white text); Welcome content wrapper `<div>` → `<main>`; HeroSearch hero sentence → `<h2>`; GuestLayout card → `<main>` + sr-only `<h1>`; 6 Auth pages get visible `<h2>` titles; Show.vue "Popularity Score" h3 → h2; dropped the `onMounted` `getCurrentPosition` (geolocation-on-start), GPS now user-gesture only. Live-verified on staging: Accessibility 100 + Best Practices 100 (mobile + desktop). Commit `dc383a0`.

2026-06-28 — Spec 061: Frontend bundle diet — `config/ziggy.php` `'only'`-allowlist of the 13 routes `route()` actually calls (inline `@routes` ~40KB→~1KB on every page); Welcome `ResultsGrid` → `defineAsyncComponent`, deferring the RestaurantCard tree off the idle homepage entry; `vite.config.js` `manualChunks` vendor split (vue + @vue/* + @inertiajs/*). `@vueuse/motion` was already removed; latin-font + 640KB logo skipped as low-value. Commit `dc383a0`.

2026-06-28 — Spec 062: Extract transition CSS — Moved the ~170-LOC transition block + `prefers-reduced-motion` override out of global `app.css` into new `resources/css/transitions.css`, imported only from `Welcome.vue` → ships as route-scoped `Welcome-*.css` (2.7KB); global `app.css` 91K→87K; auth/profile pages no longer load any of it. Behavior preserved (verbatim move). Commit `dc383a0`.

2026-06-28 — Spec 063: Enable Inertia SSR — `vite.config.js` `ssr.noExternal` made `bootstrap/ssr/ssr.js` self-contained (deps bundled into a vendor chunk alongside it; verified `node bootstrap/ssr/ssr.js` runs without node_modules); new `deploy/supervisor-ipop360-ssr.conf` + `deploy.yml` add an idempotent NodeSource Node-22-ensure step, a supervisor register step, and a post-deploy `ipop360-ssr` restart. Both first-time droplet steps passed first try. **SSR is LIVE** (initial HTML carries the hero + a pre-rendered tree in `<div id="app">`); mobile Performance **70→98** (FCP/LCP ~4.8s→1.95s); desktop 100/100/100/100. Steps are `continue-on-error` + restart `|| true`, so a future SSR hiccup falls back to CSR (no outage). `config/inertia.php` skipped (vendor default enables SSR @13714); `JsonLd.vue` left SSR-safe as-is. Commit `dc383a0`.

2026-06-29 — Spec 066: Free quality sources (relieve the SerpApi 50/mo bottleneck) — **1A** recovered Foursquare's discarded rating: added `rating_signals` to its `fields`, `normalizeOne` rescales 0-10→0-5 into `google_rating` (+ rating_signals as review count, else the Bayesian collapses v=0→Q=C and ignores the rating), tags `rating_source`, keeps raw `foursquare_rating`; gated by `sources.foursquare.use_rating`; extended `qualitySourceConfigured()` to recognize Foursquare. **1B** wired Google Places onto the live read path: `GooglePlacesService` got the full pool contract (source tag `google_places`, distinct from enrichment's `google_nearby`/`google_details`), added to `LiveSearchService` constructor + source list + the easy-to-miss `normalizeCachedHit` arm; `poolRequestsFor` bails when keyless / disabled / over `monthly_budget` (cost-metered $32/1k, not free-tier-capped — 24h cache is primary control, 500/mo backstop ≈$16, ~8% of the $200 Google credit); no bail on null cuisine (abundance on unscoped searches). **1C** authority-aware dedup: `VenuePipeline::mergeVenues` now keeps the Google rating over Foursquare when the same venue merges (rank serpapi/google_places=2 > foursquare=1), else the new Foursquare rating would've displaced the more-authoritative Google rating on merged rows. `quota:status` reports Google Places usage; `ExternalApiCache::countRealGooglePlacesCallsLast30Days()`. +15 tests (313 green), Pint clean. Safe-by-default: no-op until keys are provisioned. First of the 5-spec Coverage & Quality plan (066→067→068+069→070).

2026-06-29 — Spec 067: OSM tag broadening + free-source abundance + raise cap — **OSM broadening**: replaced the hardcoded `["amenity"="restaurant"]` in `OverpassService` (`buildQuery` + both name-regex paths `fetchByNameRaw`/`executeSearchByName`) with a configurable regex union `["amenity"~"restaurant|fast_food|cafe|bar|pub|biergarten|ice_cream"]` from `sources.overpass.amenities` (env-overridable). Overpass rows carry no `place_types`, so the tag set IS the noise guard. The amenity union is folded into BOTH Overpass cache keys (`overpass_search:` + `overpass_name:`) so a config change invalidates stale restaurant-only caches cleanly (OSM is free, so no quota cost). Raised the live `out body center` cap 50→80 (`sources.overpass.live_limit`). **Foursquare unscoped**: `poolRequestsFor` no longer bails on null cuisine (gated by `sources.foursquare.unscoped`, default on); when cuisine is null the `query` param is omitted so the API returns all nearby dining. **Raised `live_search.max_results` 30→60** so the broader recall is actually visible + pagination (068) has rows to slice. +5 tests (318 green), PHPStan 0, Pint clean. All free (OSM unlimited, Foursquare 500/mo) — no quota impact. Broadening surfaces cafes/bars on unscoped searches (filterByCuisineRelevance still drops them on scoped searches; Google Maps shows them too).

2026-06-29 — Spec 069: Ranking fidelity (phone dedup + sort-before-bound + credibility rating sort) — **4A phone dedup**: `VenuePipeline::venuesMatch` now tries a phone fast-path FIRST (last-10-digits equality + within radius, ≥10 digits required so a shared reservation line can't false-merge), bypassing the name check — so a rating attaches to its name-divergent OSM/SerpApi/Foursquare counterpart and duplicate rows collapse. Kill-switch `dedup.phone_match`; proximity extracted to a shared `withinRadius`. **4B sort-before-bound**: moved the live sort out of the controller into `VenuePipeline::sortVenues`, and `LiveSearchService::search` takes a new `$sort` param (6th) and sorts the FULL scored set BEFORE `boundResults` — fixes `?sort=nearest` silently dropping the true #N+1 nearest (bound-then-sort). Controller passes `$sort` through, dropped the redundant re-sort + removed dead `sortLiveResults`/`tiebreakLive` + the now-unused `PriceLevelNormalizer` injection (moved into `VenuePipeline`). **4C credibility rating sort**: `?sort=rating` sinks venues with < `ranking.rating_sort_min_reviews` (20) below credible ones (rating−10 bucketing) so 5.0/3 can't beat 4.8/5000; kill-switch `ranking.rating_sort_credibility`. Controller sort-logic tests converted to `VenuePipeline::sortVenues` unit tests (the logic's new home) + added credibility/phone/sort-before-bound cases. 323 tests green, PHPStan 0, Pint clean.

2026-06-29 — Spec 070: Cuisine lexicon breadth — added 5 missing cuisines (**Nepalese, Tibetan, Burmese** → asian; **Afghan** → middle-eastern; **Russian** → european) that previously hit the "honest empty" gate (`resolveScope`→isInvalid) and returned zero results. Three synced places: (1) `config/cuisine-keywords.php` cuisines (tight, cuisine-specific keyword fragments, avoiding cross-cuisine collisions like `kebab`) + category memberships; (2) `CuisineSeeder` (source for tests + fresh installs — `CuisineMatcherTest::setUp` seeds it, so the drift guards compare against it); (3) a one-time idempotent migration `2026_06_29_120000_add_breadth_cuisines` (firstOrCreate) to reach the PROD DB via `migrate --force` (deploy runs migrate, not --seed). All use firstOrCreate so no conflicts. Drift guards (`every_db_cuisine_has_a_config_keyword_set`, `config_categories_match_db_taxonomy`) pass — DB and config in sync. `CuisineMatcher`/`OverpassService::resolveCuisine` auto-pick up the new lexicon entries (no service code changed). 323 tests green, PHPStan 0, Pint clean, migration applies cleanly.

2026-06-29 — Spec 068: Live-search pagination — the live path returned a faked "page 1 of 1" (`next_page_url: null`) so users could never page past one screen. Backend-only snapshot-and-slice in `RestaurantController::apiIndex`: page 1 runs the (cache-warm/zero-quota) `search` + snapshots the full bounded set under `live_page:{md5(lat,lng,cuisine,category,sort)}` in `ExternalApiCache` (~10 min TTL), returns the first `page_size` (20) with a real `next_page_url` (`fullUrlWithQuery(['page'=>N+1])`); pages 2+ slice the snapshot (no re-search — deterministic across pages, quota-safe). The page-1 key includes `$sort` so different sort modes page independently. The frontend's existing `useRestaurantSearch.ts::loadMore()` already consumes `next_page_url` → **zero structural frontend change** (the "load more" control was already wired, just hidden by the null). Kill-switch `live_search.paginate` (default on) + knobs `page_size` (20), `page_snapshot_minutes` (10). If the snapshot expires mid-pagination → empty page + the frontend's existing "couldn't load more" state. +3 tests (326 green), PHPStan 0, Pint clean.

2026-06-29 — **Paid-source cleanup: Foursquare + Google Places + Outscraper fully removed.** Followed the spec-066 revert to its conclusion — neither Foursquare nor Google Places was contributing any real POIs in prod (deploy injects only SERPAPI_API_KEY; live results showed only serpapi + bizdata sources). Deleted `FoursquareService`, `GooglePlacesService`, `OutscraperService` (Outscraper's only consumer was the Google-Places-driven paid bonus, so it was orphaned). Read path (`LiveSearchService`) now fires 4 sources only: BizData, SerpApi, Overpass, Socrata. Enrichment (`RestaurantEnrichmentService`): Foursquare dropped from the parallel-fetch `$specs`/match arms; the Google+Outscraper `enrichPaidBonus` method + its 4 helpers (`matchGooglePlace`, `buildGooglePhotoUrl`, `mapPriceLevel`, `computeAverageBusyness`) + its call site removed; 3 constructor deps dropped. `qualitySourceConfigured()` simplified to SerpApi-only. Config: removed `services.google/outscraper/foursquare`, their cache TTLs (`google/outscraper/foursquare_ttl_hours`), `foursquare_timeout`, `sources.foursquare`. Tests: deleted `FoursquareServiceTest`; dropped Foursquare from `LiveSearchScoringTest` (pool test → 4 sources); removed the Google paid-bonus test + stripped dead google/outscraper/foursquare config+Http::fake lines from `EnrichFreeOnlyTest`/`SerpApiPersistenceAndThrottlingTest`. 301 tests green, PHPStan 0, Pint clean. **Net: zero paid-source code paths remain; only free sources (SerpApi rating + BizData/OSM/Socrata abundance + Wikidata awards).**

2026-06-30 — Spec 071: Cuisine-match scoring bonus (recall-safe re-rank) — A user's "brazilian food in Tampa" search ranked an açaí-bowl shop **Vale Healthy Kitchen** (4.6★/1,893, ~250m) at **#1** (0.7585) above a genuine **Terra Gaucha Brazilian Steakhouse** (4.8★/7,243, ~5.5km, 0.6598): all 11 results were `source:serpapi` with no exposed place_types, surviving the filters on the recall-protective "ambiguous→keep" default, then **proximity (0.20 weight, 2km scale) decided the order** since every venue rated 4.4–4.8. Fix = a new **`cuisine_match` scoring signal** that boosts venues matching the searched cuisine so genuine matches outrank borderline-nearby ones. **Drops nothing** (re-rank only, spec-046 philosophy). Two parts: (1) `LiveSearchService::stampCuisineMatchStrength` (new; called in `search()` after `filterByDistance`, before scoring) — no-op when unscoped or kill-switch `ranking.cuisine_match` off; else stamps every row `1.0` (on-cuisine keyword in NAME) / `0.5` (keyword in place_types+description) / `0.0` (scoped, no match) using the SAME `$onPattern` as `filterByCuisineRelevance` (the existing on-allowlist — not a new denylist → no spec-046 substring-collision risk); (2) `PopularityScoreService` new signal `cuisine_match` (weight 0.15, method `passthrough`, label "Cuisine Match"), `isPresent` returns `$raw !== null`. **The load-bearing design:** the scorer renormalizes weights over each row's ACTIVE signal set, so `cuisine_match` MUST be active for every row on a scoped search (value `0.0` for no-match), NOT gated per-row — gating backfires (a no-match row's quality/proximity renormalize UP and re-inflate proximity; the bowl shop still wins, proven Vale 0.769 > Terra 0.711). The `0.0`-vs-`null` distinction (0.0 = scoped-no-match → active; null = unscoped-no-stamp → inactive) is the only way to encode it since `isPresent` gets the raw value, not the row. Config: `RANK_WEIGHT_CUISINE_MATCH` (0.15) + kill-switch `RANK_CUISINE_MATCH`. `ScoreBreakdown.vue` is data-driven so it renders auto; pinned `bg-fuchsia-500`. **Live path only** — `Restaurant` models never carry `cuisine_match`, so DB/enrichment scoring is untouched (no migration); scoring runs per-request on cached reads → live instantly, zero quota. 314 tests (+8: Tampa-mirror regression, kill-switch reverts, unscoped-no-signal, recall keeps no-keyword venue, name>typed>none tiers, never-uses-rival-keywords, 0.0-vs-null invariant, config-presence), PHPStan 0, Pint clean, npm build clean. **Live-verified (commit 476ca34, GHA-green):** Tampa/brazilian → Terra Gaucha #1 (0.7041, +0.1304 Cuisine Match), Vale demoted to #2 (0.6596, +0), Bahia/Terra Mar #3/#4, Kaia Bowls #5; unscoped Tampa → 0/20 rows carry Cuisine Match (no regression). Lesson → [[scoring-signal-must-stay-active-at-zero]]. Detail: `history/2026-06-30--spec-071-cuisine-match.md`.

2026-06-29 — **Spec 066 REVERTED (stay SerpApi-only / truly free).** The "free quality sources" premise was wrong — verified against the vendor pricing pages: Foursquare's `rating`/`rating_signals`/`popularity`/`price`/`photos` are **premium-tier fields → $18.75/1k from call 1, no free tier** (the "0–500/10k free" is Pro/default-fields only); Google Places Nearby Search is **~$32/1k** (free-for-low-volume via the $200/mo credit BUT card-on-file + metered). The repo's constitution/memory "Foursquare Places 500/mo free" was stale. Per the user's decision, reverted the entire spec: Google Places off the read path (restored `GooglePlacesService` to enrichment-only; removed from `LiveSearchService` ctor/source list/`normalizeCachedHit`); Foursquare rating recovery undone (`normalizeOne` discards the rating again, `rating_signals` dropped from `fields`); authority-aware dedup + `rating_source` removed from `VenuePipeline`/`SerpApiService`; `qualitySourceConfigured()` restored; Google Places budget counter + `quota:status` block + `sources.google_places`/`sources.foursquare.use_rating`/`google_places_timeout` config removed; `GooglePlacesServiceTest` deleted; the Foursquare-rating + authority + google-places tests removed. **Preserved 067/068/069/070.** Residual: Foursquare's *pre-existing* query still requests premium fields (rating discarded) → a `FOURSQUARE_API_KEY` would still bill for unrated abundance (no-op without a key; flagged, not changed). 311 tests green, PHPStan 0, Pint clean. **Net: SerpApi (~50/mo) is again the only rating source; genuinely-free abundance (OSM, spec-067) stands.**

2026-06-30 — **Full-app audit → specs 072–079 (8 specs, P1 backlog shipped).** An 8-dimension read-only audit workflow (Security / Backend / Perf / Frontend / Testing / Architecture / Infra / Data-Ranking; finders → adversarial verify) raised 52 findings, **49 confirmed real** (7 P1, 23 P2, 19 P3). A live SerpApi quota email (**188/250 used mid-cycle**, quota is **250/mo not the assumed 50**) made the availability/security findings urgent and reprioritized them first. Per the user's directive ("implement all 7" P1s + the cache-key fix the diagnosis surfaced), shipped 8 specs as `feat(spec-NNN)` commits (`292e3e6`…`d1dfbd1`), then hardened the batch with a 5-dimension adversarial review (`cd0609c`) that found+fixed 4 HIGH issues. **344 tests green (+30), PHPStan 0, Pint clean, build OK.** Each fix is kill-switched + recall-protective per the project values.
- **072 — unify SerpApi cache keys:** enrichment's skip-check (`query` compact key) and store (`cuisine` compact key) diverged → the freshness check never saw its own store → enrichment re-fetched every city×cuisine combo every nightly run, and never pre-warmed the read path. Fix: enrichment builds every source's key via that source's `cacheKeyFor` with the read-path-canonical value (`humanize(slug)` for query sources, slug for Overpass). `isSerpApiCacheFresh` delegates to `SerpApiService::cacheKeyFor`.
- **073 — read-path quota guard + coord rounding:** `cacheKeyFor` rounds lat/lng ~3dp IN THE KEY (fetch keeps raw) so GPS jitter stops minting fresh keys; `LiveSearchService::allowLiveSerpApiFetch()` adds a monthly circuit breaker (30d calls ≥ 0.8·`free_quota` → cache-only) + per-IP hourly limiter (`live_misses_per_hour`). `serpapi.free_quota` corrected **50→250**. Recall-safe (warm cache unlimited; tripped guard → free sources still answer). Kill-switch `SERPAPI_READ_PATH_GUARD`.
- **074 — thundering-herd `Cache::lock`** on the SerpApi cache-miss (review-fixed to scope the lock to a **SerpApi-only** fetch+store, not the whole pool, so a slow Overpass leg can't keep it held past the waiter timeout).
- **075 — SSRF sandbox** for the website scraper (`isSafeUrl`: resolve host, reject private/loopback/link-local/metadata IPs, http(s) only; runs before robots.txt; shared `redirectOptions()` re-validates each redirect hop on BOTH the robots.txt and page fetches — review-caught the robots.txt bypass). Kill-switch `WEBSITE_SCRAPER_SSRF_GUARD`.
- **076 — Pulse gate** → email allow-list (`PULSE_ADMIN_EMAILS`) in non-local env (was "any logged-in user" + open registration).
- **077 — pre-migration DB backup** (`db:backup` artisan command, `VACUUM INTO` + rotation) + deploy.yml `artisan down`/`up` maintenance window (review-fixed: `up` always succeeds via `sudo -u www-data … || sudo rm -f storage/framework/down`; can't wedge the site) + `busy_timeout` 5000ms.
- **078 — fix O(n²) score-breakdown fallback** in `RestaurantResource`: compute normalization aggregates ONCE at the controller level (`withAggregates`), not per-row (the favorites page was the worst case — unbounded, all-NULL breakdowns). Byte-identical math (regression test). Favorites pagination deferred.
- **079 — carry `place_types`+`description` through `VenuePipeline::mergeVenues`** so spec-071's `cuisine_match` stamp isn't zeroed when a rich SerpApi row folds into a name-only OSM/BizData target.
- **Tracked follow-ups (not blocking):** SSRF DNS-rebinding TOCTOU + IPv6 dual-stack bypass (need IP-pinning via CURLOPT_RESOLVE); favorites per-user cap/pagination; Socrata grade-string in merged description; circuit-breaker counting expired rows; `stats()` query cost on the miss path. The P2/P3 backlog (ranking-correctness cluster, frontend a11y/races, dead-code, Vitest=064) stands.
**Status at this entry: committed locally, NOT yet pushed.** Source of truth: `specs/072-…079` + `.specify/memory/project-state.md`.

2026-06-30 — **Ranking-correctness P2 cluster → specs 080–083.** The audit's highest-value remaining cluster (the product's core domain), shipped as 4 recall-protective specs (`16ce6cc`…`0bf7e1f`) + a focused adversarial review fixing 2 medium / 3 low findings (`c011d7f`). **353 tests green (+9), PHPStan 0, Pint clean.** Each is recall-protective + kill-switched (where behavioral).
- **080** — `CuisineMatcher::rivalKeywords()` skips rival keywords that are a proper substring of an on-cuisine keyword (curry⊂curry.goat, roti⊂roti.canai/sel.roti, pita⊂spanakopita, kaya⊂izakaya, milan⊂milanesa). Same class as the spa⊂spanish lesson (spec-046); stopped false-dropping genuine on-cuisine venues described only by a shared term (e.g. a Jamaican "Caribbean Curry" place).
- **081** — `PopularityScoreService::collectionMeanRating()` excludes (0,0) null-island rows from the Bayesian credible mean C (reuses the filterByDistance predicate). A phantom rating no longer pollutes the mean that shrinks every venue's quality (uniformly, so invisible in tests).
- **082** — `LiveSearchService::scoreWithUnifiedService()` gives no-coords rows NEUTRAL proximity (sentinel = proximity_scale_km → ~0.5, stamped for scoring then unset so the card shows no fake distance), so per-row weight renormalization no longer inflates their other signals. Kill-switch `RANK_NO_COORDS_NEUTRAL_PROXIMITY`. Review-fix: the sentinel now overrides (0,0) rows' pre-set huge distance too (consistent with 081).
- **083** — `SocrataOpenDataService::deduplicateByName()` keys on name + coords (~4dp) instead of derived distance (stopped collapsing distinct same-named venues at coincidentally-equal distance — a recall loss on the NYC/SF source); dropped the `round(null ?? 0)` footgun. Review-fix: no-coords rows key on name alone; (0,0) treated as no-usable-coords.
- **Review follow-ups (not blocking):** the pho⊂pholourie precision/recall trade is documented (drift-guard case); the (0,0) ⊂ no-usable-coords predicate is now consistent across filterByDistance/081/082/083.
**Status: committed locally, NOT yet pushed.** Source of truth: `specs/080-…083` + `.specify/memory/project-state.md`. Remaining backlog: the other P2 clusters (frontend a11y/races, ~1500-LOC dead-code, infra/scheduled-jobs) + spec-064 (Vitest).

2026-06-30 — Live-verify specs 072–083 (both audit waves now pushed + GHA-green + behaviorally verified). Prior notes ("committed locally, NOT yet pushed" / "pending push+live-verify") are now STALE — both waves are on master and deployed green (GHA "Deploy to Staging" success on `ce1ba29`). Verified via the live API (https://ipop360.vp-associates.com/api/restaurants) + raw SSR HTML, because both browser MCPs were unavailable this session (chrome-devtools: no X server; playwright: profile locked by another session — the documented spec-071 fallback). **Quota discipline:** only cache-COLD queries burn SerpApi units; Mobile unscoped (~9.7s), Mobile `sort=nearest` (~8.4s, different cache key), and Mobile/chinese (~22s) were cold → ~3 calls burned (~59 of 250 remaining, renews 2026-07-19); all further checks used DB-served or now-warm queries (zero quota).
- **080 (rival-keyword substring collisions):** Mobile/chinese (cuisine-scoped → exercises `CuisineMatcher::rivalKeywords()`) returned **13 all-Chinese, all-restaurant, all-rated serpapi results** — recall intact, no over-drop, no non-restaurant leak (042/046 holding). Matches spec-027's canonical "11 all-Chinese" baseline (now 13).
- **081 (exclude (0,0) from quality mean):** no null-island rows in any live set checked; pure numerical Bayesian-mean change (not browser-observable). Verified by green suite (353) + adversarial review. Mobile Noja Quality signal normalized 0.9559 (sensible, unpolluted).
- **082 (no-coords neutral proximity):** Mobile live coords rows get a REAL proximity signal (Noja score-breakdown Proximity normalized **0.8026**, contribution 0.1605) — the ~0.5 neutral sentinel is correctly reserved for no-coords rows, NOT mis-applied to coords rows. `sort=nearest` returned closest venues first (Ruby Slipper @30.6941,-88.0413 ≈0.2km → out) → distance IS computed internally for sort/score. No sentinel leaks to the API `distance` field.
- **083 (Socrata dedup on coords):** Socrata is the NYC/SF source; unscoped NYC DB-intercepts (persisted rows) so it wasn't cheaply exercisable live without burning quota on a cold NYC cuisine search. Verified by green suite + the spec-083 regression test + adversarial review (recall-protective; no observable "before" baseline live anyway).
- **072–079 sanity on the live path the cluster rides on:** 072 unified cache keys → live search returns rated serpapi results ✅; 073 read-path guard did not trip (results returned normally) ✅; 068 pagination → real `next_page_url`, total=60, 3 pages ✅; 078 → `score_breakdown` present on every row (Noja full 6-signal breakdown) ✅. SSR (063) live — hero "iPop360"/"Discover top-rated restaurant" in the INITIAL HTML; SEO meta (038) + canonical/OG present; route-scoped `Welcome-*.css` chunk served (062).
- **Follow-ups surfaced (NOT regressions, not blocking):** (1) the `distance` field is `null` on all LIVE API results (DB-served rows like Austin carry real distances) — pre-existing; live rows compute distance internally for sort/score but `RestaurantResource.php:68` only serializes `$this->resource->distance` when non-null, and the live array path doesn't surface it the way the DB `nearby()` alias does. Worth a look (frontend card may not show distance for live results) but untouched by 080–083. (2) JSON-LD is NOT in the SSR HTML — expected, since `JsonLd.vue` injects client-side (Inertia `<Head>` drops `<script>`); server-rendering JSON-LD is a P3 opportunity now that SSR is live. (3) Couldn't browser-verify JSON-LD rendering (needs JS exec; both browser MCPs unavailable).
**Status: 072–083 PUSHED + GHA-green + live-verified.** Source of truth: this file + `.specify/memory/project-state.md`. Next: spec-064 (Vitest).

2026-06-30 — Spec 064: Frontend test tooling (Vitest) — the project's FIRST frontend tests (was zero; only vue-tsc gated the build). Added Vitest 4.1.9 (peers Vite ^8) + jsdom 29 + @vue/test-utils 2, a `vitest.config.ts` mirroring the `@`/`ziggy-js` aliases + jsdom env, an `npm run test`/`test:watch` script, and a `Run frontend tests (Vitest)` step in the CI quality gate. Test files are EXCLUDED from the `vue-tsc` BUILD typecheck (`tsconfig.json` `exclude`) so a test-only type drift can never break the deploy gate; vitest runs them via esbuild. First tests (52, green) target pure lib helpers + composables: `lib/utils` (cn/tailwind-merge), `lib/cuisine` (exact-gradient lookup), `lib/restaurant` (maps/directions URLs, openWebsite scheme, callPhone via a window.location Object.defineProperty stub — jsdom locks Location.href), `lib/api` (buildParams incl. the 0/false/'' falsy-but-valid contract, fetch wrapper success/4xx/**network-throw**/FormData/caller-header-merge), `useSeo` (4 JSON-LD generators + canonical-URL stripping), `usePersistedLocation` (city+coords round-trip, legacy city-only, corrupt-JSON, city-null/coords quadrant), `useFavorites` (guest localStorage + authed optimistic + **rollback**), and a `// @vitest-environment node` file for the SSR no-window fallback (`useBaseUrl`/`getBaseUrl` → prod URL, the spec-063 SSR guard). Hardened by a 3-lens adversarial review workflow (assertion-strength / mock-validity / coverage-gaps; 3 agents, 14 findings) — tightened the cuisine assertions to exact-string equality (one was a substring match that 5 gradients satisfy; another was a tautological `toBe(itself)`), made the JSON-LD address block a full `toEqual`, pinned `buildParams`' 0/false/'' contract and `api()`'s network-throw + caller-header-merge branches, reset the callPhone stub between tests. The Inertia `usePage()` mock uses a **reactive** page stub so Vue computeds re-track the optimistic mutations (a plain object cached stale — proven by the failing-first authed test). **Source bug the tests surfaced (fixed in the same spec, `0ac3480`):** `useFavorites`'s optimistic-toggle rollback was a NO-OP — the optimistic write mutated `page.props.auth.favorites` before the `await`, so by the time `onError`/`catch` ran, `serverFavoriteIds.value` had already re-evaluated to the new value and both rollback branches assigned it back to itself; on a failed toggle the UI kept a favorite the server rejected. Fix: snapshot `previousFavorites` before the optimistic write and restore THAT. Two new regression tests (onError + network-reject) fail against the old source. 52 frontend tests + 353 backend tests green; PHPStan/Pint unaffected (frontend-only); `npm run build` clean. Commits `191dc55` (feat) + `0ac3480` (fix). **Tracked follow-ups (NOT fixed — outward-facing, need browser live-verify):** (1) `openWebsite` mangles non-http schemes (`ftp://x` → `https://ftp://x`); (2) `useSeo` canonical stripping keeps URL fragments (`#reviews`) — a duplicate-content SEO smell. Both 1-line fixes when a browser session is available to verify. spec-064 was the LAST open spec → **the queue is now EMPTY (001–084 all COMPLETE/SHIPPED, 066 reverted).**

2026-06-30 — Spec 085: Favoriting a live result no longer 500s / leaks an orphan row — Every live-source result embeds a synthetic placeholder cuisine id `abs(crc32('restaurant'))` = 3952415295 (SerpApi/Socrata/BizData `normalizeResults`) that does NOT exist in the `cuisines` table (real ids <50). `FavoriteController::ensurePersisted()` ran `$restaurant->cuisines()->sync($cuisineIds)` on it with no existence check and no `DB::transaction`, so the `cuisine_restaurant` pivot FK (`foreign_key_constraints=true`) rejected it as an uncaught 500 — and the already-committed `Restaurant::create()` was left as an orphan row (re-favoriting made another). `FavoriteControllerTest` only ever toggled venues with a real `google_place_id` and no `cuisines` array, so CI was blind; since most served results are `is_live:true`, essentially every first-time favorite of a live venue 500s — a core flow broken. Fix (recall-protective, durable): new `resolveCuisineIds()` keeps only ids present in `cuisines` (synthetic/decorative ids dropped — a live result's `slug:'restaurant'` tag is decorative, correct not to attach); create + cuisine-attach wrapped in `DB::transaction`. **Scope widened per the adversarial review's 1 confirmed LOW finding:** `merge()`'s venue loop + `syncWithoutDetaching` are now wrapped in an OUTER `DB::transaction` so a mid-merge failure can't leave committed-but-unfavorited orphan rows — same invariant, same fix class (each `ensurePersisted` keeps its per-venue nested/savepoint tx); pre-existing gap the review flagged as blast-radius in the code this spec touched ("no code change required to ship"), closed so the orphan-invariant holds across BOTH favorites write paths. 6 regression tests (live-shape toggle → 200/persisted/0-bogus-cuisines; mixed real+synthetic → real kept, synthetic dropped; idempotent re-favorite → no orphan; merge w/ synthetic cuisine; single-venue forced-failure rollback; mid-merge forced-failure rollback). Both halves reproduced pre-fix: 4 tests → `500 / FOREIGN KEY constraint failed`; rollback test → orphan count `1` vs expected `0`. 359 backend tests (+6), PHPStan 0, Pint clean, build OK. Hardened by a 5-lens + per-finding-verify adversarial review (4 raised, 3 refuted, 1 confirmed-LOW → addressed). **Live-verified** (live-API flow; browser MCPs unavailable again this session — no X server / profile lock, the spec-071 fallback): registered a throwaway user, fetched a real `is_live:true` Mobile/chinese result ("China Chef II", `cuisines:[{id:3952415295,…}]` = the synthetic id, restaurant `id:-3669859679` negative → create path), POSTed to `/favorites/toggle` exactly as `useFavorites` does → `{"favorited":true,"favoriteIds":[927]}` HTTP 200 (was 500 before); zero quota (Mobile/chinese warm from spec-080). Commit `e20f9a5`, GHA-green (Deploy to Staging run `28478026339`). P1 wave: 085 ✅, next 086 → 087. Source of truth: `specs/085` + `.specify/memory/project-state.md`.

2026-06-30 — Spec 086: Deploy "Verify deployment" asserts HTTP 200 + non-empty payload — The API verify was `curl -s URL | python3 -c "exit(0 if 'data' in d else 1)"` — it checked ONLY that a `data` key existed, not HTTP status, not non-empty, so a valid 200 with `{"data":[]}` (a cache-nuking migration or an over-filtering ranking change) shipped green. Fix: the API check now captures the HTTP code (`curl -o /tmp/api.json -w '%{http_code}'`) and asserts 200, then python asserts the `data` key exists AND `len(data) >= DEPLOY_VERIFY_MIN_RESULTS` (default 5, from the `vars.` repo variable; Mobile/chinese historically ~13-30, so min=5 is recall-safe vs a transient SerpApi quota dip). `DEPLOY_VERIFY_MIN_RESULTS=0` skips the COUNT check only (kill-switch); the 200 + data-key checks always run — a deliberate refinement of the spec's literal "reverts to key-only" AC (a non-200/keyless API is never shippable). Hardened per a 5-lens adversarial review (6 LOW confirmed, 5 refuted): (a) BOTH verify curls bounded with `--connect-timeout 10 --max-time 30` (3 lenses independently flagged the timeout-less curl — a hung/cold connection now fails fast instead of stalling to the 15-min job ceiling; a timeout yields code `000` which the existing `!= 200` / `grep -q 200` guards already fail); (b) `isdigit()` parse so a typo'd negative/garbage `DEPLOY_VERIFY_MIN_RESULTS` falls through to the safe default of 5 (the earlier `lstrip('-').isdigit()` let `-5` silently disable the count check). Tracked follow-up (no code change): a spec-073-breaker-tripped + thin-free-sources verify could dip below 5 — the `DEPLOY_VERIFY_MIN_RESULTS=0` kill-switch + `ExternalApiCache::stats()['serpapi_calls_last_30d']` triage field are the documented escapes (future spec could surface the breaker state in the failure message). Validated: 12 mock-curl end-to-end cases pass (empty / non-200 / no-data-key fail; mn=0 relaxes count; negative `-5`/`--5`/`abc` → default 5, count stays active); YAML clean, `bash -n` OK. Live-verified: the `1f1984e` deploy's OWN Verify step ran the new assertions on the real prod API → `DEPLOY OK` + `API OK: 14 results (min=5)` (Mobile/chinese); a `{"data":[]}` deploy would now FAIL this step. Commit `1f1984e`, GHA-green (Deploy to Staging run `28480005424`). P1 wave: 085 ✅ 086 ✅, next 087. Source of truth: `specs/086`/`project-state.md`.
