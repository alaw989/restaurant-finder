# iPop360 History

2026-06-19 — Kill Dead Yelp Weights: Set Yelp ranking weights to 0 after Yelp removal, all tests pass
2026-06-20 — Parallel Source Fetch: Implemented concurrent fetching for BizData/Foursquare/Overpass sources, reducing live-search latency from sum to slowest source only
2026-06-20 — Scoring Schedule & Cache GC: Chunked ScoreRestaurants (500/chunk, transactional batch updates), added apicache:gc command with --dry-run, scheduled daily scoring (02:00 UTC) and nightly GC (03:00 UTC), expanded enrichment to all configured cities
2026-06-20 — Docs Reconciliation: Updated ranking-metrics.md (weight table, free-source landscape, data_completeness fields), marked Proximity DONE in ranking-improvements.md, updated README.md Key Services
- Spec 012: SerpApi Source — Wired SerpApiService into parallel fetch pools (LiveSearchService, RestaurantEnrichmentService); service already existed with key gating and normalization
2026-06-21 — Spec 017: SerpApi Quality Signal + Weight Recalibration — Enabled the validated free SerpApi key, recalibrated weights so rating+reviews lead (0.30/0.25) and proximity is a tiebreaker (0.15), fixed 3 dormant bugs (SerpApi invalid ll zoom, compact() radius, quality-gate keyed on wrong API), added `search:audit` command; live search now ranks real rated restaurants (e.g. Gus's World Famous Fried Chicken) instead of nearest OSM nodes
2026-06-21 — Spec 019: Bayesian Quality Ranking + SerpApi Cache — Replaced separate rating/review signals with a single Bayesian `quality` signal (shrinks low-review outliers toward the credible mean; 0.60 weight), demoted completeness to 0.05, extended the SerpApi cache 24h→30 days (1 call per unique city, repeats free), neutralized the fake SF seed; 157 tests green. search:audit now shows one Quality signal leading the breakdown
2026-06-21 — Spec 018: Dedup OSM Sources + Filter Garbage Names — Added robust cross-source dedup (fuzzy name similarity ≥85% + haversine ≤200m) to LiveSearchService and RestaurantEnrichmentService; added garbage name filter for OSM-derived sources (numeric-only, generic cuisine words, quote-wrapped, price-leading); moved thresholds to config; 171 tests green
2026-06-21 — Spec 020: Multiple Sort Modes — Added ?sort= parameter (best_match|nearest|rating|reviews|price) to both Inertia and API paths; created PriceLevelNormalizer service for price_range → level 1-4 mapping; added sort dropdowns to Welcome.vue and Restaurants/Index.vue; nearest falls back to best_match without coords; all 195 tests green
2026-06-21 — Spec 021: Throttled DB Enrichment + Persist google_rating/review_count — Fixed bug in normalizeSerpApiVenue() and processFreeVenue() that was discarding SerpApi-sourced rating fields; added throttling mechanism (per_run_cap, monthly_budget config knobs) to enrichAllCitiesThrottled() with cache-aware rotation; scheduled daily throttled enrichment (04:00 UTC); all 201 tests green
2026-06-21 — Spec 022: Cache & SerpApi Quota Observability — Added read-only `ExternalApiCache::stats()` method and `quota:status` artisan command to surface last-30d burn vs 50/40 budgets plus cache inventory; added `serpapi.free_quota` config (50); all 221 tests green
2026-06-21 — Spec 023: Live-Search Feedback States — Added distinct error states for Welcome.vue search failures (different from empty results) with retry affordance; added loadMore error state that preserves existing results with inline error/retry; added geolocation error banner; wired Skeleton component in Restaurants/Index.vue for in-flight sort/navigation; ralph-loop.sh gained VERIFY_BUILD gate (npm run build) for frontend spec safety; all 221 tests green, npm run build passes
2026-06-21 — Spec 024: Enrichment Robustness — Persisted Google Places 'website' to website_url (unblocks website scraper → opening_hours); added exponential backoff retry (3 attempts, 100ms base) to RestaurantWebsiteScraperService::performScrape() and SocrataOpenDataService::fetchEndpoint() for transient HTTP failures; added 'model' field to ai_metadata in EnrichRestaurantWithAi job; all changes preserve graceful null/[] returns, no new API calls; all 221 tests green
2026-06-25 — Spec 025: Real Http::pool Concurrency for Live-Search Fetch — Replaced the fake "parallel" closure-thunk fetch (built per-source thunks then invoked them serially → wall time = SUM of sources) with true Http::pool() concurrency via a readonly RequestSpec VO + per-source cacheKeyFor()/poolRequestsFor()/consumePoolResponses(); fetchAndMergeAllSources() now 3-pass (synchronous cache lookup → pool the misses → consume hits + pool results); tightened live-path timeouts in config('restaurant-finder.live_search') (Foursquare gets explicit timeout, Overpass drops to first radius/mirror, Socrata drops 3x retry) while the DB-enrichment fetchRaw() path keeps generous defaults; replaced the weak concurrency test with a structural guard (pool interface used, fetchRaw NOT called on the live path) + a new failure-isolation test; 222 tests green
2026-06-25 — Spec 025 fix: Bound Overpass Name Fallback on Live Path — First spec-025 deploy FAILED the verify gate (504 on a cache-cold chinese cuisine search) because applyOverpassNameFallback() called fetchByNameRaw() with the full 3-radii x 3-mirror serial fan-out (30s each) — a pre-existing latent path exposed when the 24h Overpass cache expired; bounded it on the read path (one mirror, one radius, live_search.overpass_timeout 10s) via a new $context param, enrichment path keeps the fan-out; added a one-request guard test; 223 tests green, deploy verify passed
